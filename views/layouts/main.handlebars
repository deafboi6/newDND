<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>newDND</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    <header>
      <h1>newDND H1 Header</h1>
    </header>
    <nav>
      <a href="/">Homepage</a>
      {{#if logged_in}}
        <a href="/create" id="hero-create">Create Hero</a>
        <a href="" id="logout">Logout</a>
      {{else}}
        <a href="/login">Login</a>
      {{/if}}
    </nav>
    <main>
      {{{body}}}
    </main>
    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>