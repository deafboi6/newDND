<link rel="stylesheet" href="/css/homepage.css">
      {{#if logged_in}}
        <section class="container">
          {{#each heroes as |hero|}}
            {{> hero-card}}
          {{/each}}
        </section>
      {{/if}}